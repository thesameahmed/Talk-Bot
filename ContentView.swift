import SwiftUI
import AudioKit
import AudioKitUI
import AVFoundation
import CoreData


struct ContentView: View {
    
    
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            
            Text("""
I said im coming for the crown - mothaf***ka - hold it down... like some buttons - I push down -
to go and play my sounds - that I brought in - through the cloud - thats exhaustive - figure out - what it cost him - to put out all this content.

""")
            
            
            .multilineTextAlignment(.center)

            
            
            
        }
        .padding(50)
        .border(.black, width: 10)
        
    }
}
